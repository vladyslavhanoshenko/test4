@model DAL.Entities.Station

@{
    ViewBag.Title = "EditStation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <h3>Station # @Model.Id  </h3>
    @using (Html.BeginForm("EditStation", "Home", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.HiddenFor(m => m.Id)
        <div class="form-group">
            @Html.LabelFor(m => m.X, "X", new { @class = "col-md-2 control-label" })
            <div class="col-md-4">
                @Html.TextBoxFor(m => m.X, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Y, "Y", new { @class = "col-md-2 control-label" })
            <div class="col-md-4">
                @Html.TextBoxFor(m => m.Y, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-4">
                <input type="submit" value="Edit" class="btn btn-success" />
                <input type="button" name="action" value="Cancel" onclick="location.href='@Url.Action("Laboratories", "Home")'" class="btn btn-default" />
            </div>
        </div>
    }
</div>
